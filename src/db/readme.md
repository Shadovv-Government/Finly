```
transactions — все доходы и расходы.
categories — справочник категорий (системные + пользовательские).
budgets — лимиты по категориям.
goals — финансовые цели («копилки»).
recurring_templates — шаблоны для повторяющихся операций (для автодобавления).
settings — настройки приложения (тема, валюта, курсы) и данные для AI (паттерны обучения).
```